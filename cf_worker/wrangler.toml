name = "rcc-pc-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"
workers_dev = true

# Secret (NOT committed) — set via:
#   wrangler secret put PC_API_TOKEN
# (paste the token when prompted)
